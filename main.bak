#lang racket/base

(require twitch-bot
         unreal)

;Switch to fish tank Build
(bootstrap-unreal-js  
 "Build\\WindowsNoEditor\\FishTanks\\Content\\Scripts")

(start-unreal 
 "Build\\WindowsNoEditor\\FishTanks.exe")

(define (prep-for-chat-output v)
  (if (unreal-actor? v)
      "[Unreal Actor]" ;Snip it.  Too long
      v))

(start-twitch-bot
 (handle-twitch-message
  (lambda (expr)
    (prep-for-chat-output
     (safe-lang-evaluation
      'fish-tank/chat
      expr)))))
